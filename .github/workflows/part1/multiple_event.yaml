name: multiple_event-workflow
#하나의 workflow에서 다양한 이벤트 트리거를 위함
on:
  push:
  issues:
    types: [opened]
  workflow_dispatch:

jobs: #실행 시킬 job집합
  multiple-evet-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo hello world
      - name: step2
        run: |
          echo hello
          echo jongbin
