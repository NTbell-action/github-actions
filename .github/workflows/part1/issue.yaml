name: issue-workflow 1
on:
  issues:
    type: [opened]

#issues경우 타입이 많음, 오직 default branch에서만 트리거됨!!
# "opened, edited, deleted, transferred, pinned, unpinned, closed, assigned"
# "unassigned, labeled, unlabeled, locked, unlocked, milestoned, demilestoned"

jobs:
  push-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo hello world
      - name: step2
        run: |
          echo hello
          echo jongbin
