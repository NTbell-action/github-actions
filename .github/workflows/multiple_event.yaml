name: multiple_event-workflow
on:
  push:
  issues:
    types: [opened]
  workflow_dispatch:

  jobs: #실행 시킬 job집합
  multiple-evet-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo hello world
      - name: step2
        run: |
          echo hello
          echo jongbin
