# name: artifact
# # 워크플로우 실행 중 생성된 파일 또는 파일 모음
# # 동일한 워크프로우 내에서 job 사이에 데이터 공유
# # 워크플로우가 종료된 후에도, 데이터 유지 (90일)
# # 필요시 다운가능, upload, download - artifact
# #빌드 결과물, 테스트 리포트, 로그 등을 저장하고 워크플로우 실행 후에도 다운로드하거나 공유할 때 사용

# on: push

# jobs:
#   upload-artifact:
#     runs-on: ubuntu-latest
#     steps:
#       - name: echo
#         run: echo hello-world > hello.txt
#       - name: upload artifact
#         uses: actions/upload-artifact@v3
#         with:
#           name: artifact-test
#           path: ./hello.txt

#   download-artifact:
#     runs-on: ubuntu-latest
#     needs: [upload-artifact] #업로드가 완료되어야지 실행가능하므로 need 종속성 설정
#     steps:
#       - name: download artifact
#         uses: actions/download-artifact@v3
#         with:
#           name: artifact-test
#           path: ./
#       - name: check
#         run: cat hello.txt
