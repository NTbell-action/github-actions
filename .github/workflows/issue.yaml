name: issue-workflow
on:
  issues:
    type: [opened]

#issues경우 타입이 많음
# "opened, edited, deleted, transferred, pinned, unpinned, closed, assigned"
# "unassigned, labeled, unlabeled, locked, unlocked, milestoned, demilestoned"

jobs:
  push-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo hello world
      - name: step2
        run: |
          echo hello
          echo jongbin
